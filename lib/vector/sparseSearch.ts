export async function searchSparse(
    query: string,
    subjectCode: string, 
    classLevel: string, 
    chapterKey: string, 
    topK: number = 12
) {
    // Sparse index generation is not enabled yet
    return [];
}
