export default function Page() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'sans-serif' }}>
      <h1>Application Reset</h1>
      <p>All core files were deleted, so the application has been reset to a minimal state.</p>
    </div>
  );
}
